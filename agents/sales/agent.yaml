# V3 page 14 — copy this folder, rename, edit yaml + CLAUDE.md, register. Live in 90 seconds.
name: sales
role: outreach_specialist
model: claude-sonnet-4-6
voice: Aoede

tools:
  - hive_query
  - hive_write
  - skill
  # add tool names from your allowlist here

allowlist:
  hive_mind: read_write

obsidian_vault: vault/Sales/

rules: []

hard_refusals: []

hive_write_format: |
  agent=sales
  task=<one-line>
  outcome=<status>
